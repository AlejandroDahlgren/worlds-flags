import React from 'react';

const SearchRegion = ({ region, setRegion }) => {
  // Komponentens logik för filtrering efter region
  return (
    <div>
      {/* Din kod för att hantera filtrering efter region */}
    </div>
  );
};

export default SearchRegion;
